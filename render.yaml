services:
  - type: web
    name: suggest-relay
    runtime: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3010
      - key: ALLOWED_ORIGIN
        value: "*"
    healthCheckPath: /health
